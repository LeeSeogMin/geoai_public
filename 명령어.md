# 1 환경설정 바이브 코딩


lecture_practice/README.md의 3~5단계를 참고해서 이 수업의 실습 환경을 설정해줘.

내 운영체제는 Windows 11이다. 조건:

1. 프로젝트 루트에 .venv 가상환경을 만들어라.
   이후 모든 설치와 실행은 activate에 의존하지 말고
   .venv 안의 python을 직접 지정해서 실행해라.
   (Windows: .venv\Scripts\python / macOS: .venv/bin/python)
2. 패키지는 lecture_practice/requirements-student.txt 로 설치하고,
   이어서 python lecture_practice/setup_torch.py 도 실행해서
   딥러닝(5~7장) 환경까지 지금 준비해라.
2. 설치가 끝나면 lecture_practice/check_env.py 를 실행해라.
   실패 항목이 있으면 한 번에 하나씩 고치고, 고칠 때마다 다시 실행해서
   "모든 항목 통과"가 나올 때까지 반복해라.
   무엇이 실패했고 어떻게 고쳤는지 항목별로 보고해라.
3. 마지막으로 lecture_practice/chapter1/code/1-1-geoai-tools-preview.py 를
   실행해서 "전체 국가 수: 177"이 나오는지 확인하고 결과를 보여줘.
4. 끝나면 VS Code에서 내가 직접 해야 할 일이 남았는지 알려줘.
   (예: Python: Select Interpreter에서 .venv 선택)


# 2 과제실험 바이브 코딩

lecture/chapter1.md의 17절(과제)을 참고해서 1장 과제를 실행해줘.
내 학번은 [학번], 이름은 [이름], 운영체제는 [Windows 11 / macOS]다. 조건:

1. .venv 안의 python으로 다음을 실행해라.
   python scripts/submit.py 1 --id [학번] --name [이름]
2. 실행이 실패하면 오류 메시지 전체를 보여주고, 원인을 한 번에
   하나씩 고친 뒤 다시 실행해라. 무엇을 고쳤는지 보고해라.
   그래도 안 되면 실패 상태 그대로 두고 멈춰라 —
   오류가 담긴 제출 파일도 유효한 제출이다.
3. 성공하면 submissions/ 폴더에 제출_1장_[학번].md 가 생겼는지 확인하고,
   파일에 담긴 실행 결과(국가 수, 좌표계, 기하 유형, 경고)를 요약해서 보여줘.
4. 결과에 대해 내가 이해해야 할 것을 도와줘. 단, 방식은 설명과 질문이다:
   - 경고 문구가 무엇을 가리키는지, 좌표계와 기하 유형이 무슨 뜻인지는
     내가 물으면 설명해라.
   - 파일 맨 아래 "여기만 채우세요"의 빈칸 4개는 절대 대신 작성하지 마라.
     초안을 써 주지도 마라. 대신 내가 쓴 답을 보여주면
     "근거가 화면의 어떤 값인지"를 되물어 점검해줘.
5. 이 과제는 난수를 쓰지 않아서 교재의 값(전체 국가 수 177)과
   같아야 정상이다. 내 결과가 다르면 그 사실을 알려주고,
   왜 다른지 원인 후보를 3개 제시해라. 값을 교재에 맞춰 고치지는 마라.
