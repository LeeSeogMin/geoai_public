# GeoAI 실습 - 딥러닝 추가 설치분 (5-7장)
# ---------------------------------------------------------------
# 5장에 들어갈 때 설치한다. 내려받는 용량이 크므로(수백 MB에서 수 GB) 학교 밖에서,
# 시간 여유가 있을 때 미리 해 두는 편이 낫다.
#
# 이 파일을 pip으로 직접 설치하지 않는다. 대신 아래를 실행한다.
#
#   python practice/setup_torch.py
#
# 컴퓨터마다 받아야 할 PyTorch 빌드가 다르기 때문이다(NVIDIA 그래픽카드가 있으면
# CUDA 빌드, 애플 실리콘이면 기본 빌드, 그 외에는 CPU 빌드). setup_torch.py가
# 컴퓨터를 살펴보고 아래 버전을 맞는 저장소에서 받아 깐다.
#
# 그래픽카드가 없어도 실습은 모두 할 수 있다. 실습 코드는 CUDA -> MPS -> CPU 순으로
# 쓸 수 있는 장치를 알아서 고른다.
#
# 검증 기록
#   Windows 11 / Python 3.12.7 : 설치 성공, import 및 CPU 동작 확인
#                                (torch.cuda.is_available() == False 상태로 정상)
#   macOS(Apple Silicon)       : 같은 버전으로 아직 설치 검증 안 함.
#                                맥에서 설치가 막히면 버전 고정을 풀고
#                                pip install torch torchvision 로 시도한다.
# ---------------------------------------------------------------

torch==2.13.0
torchvision==0.28.0
