# GeoAI 실습 - 학생 배포용 고정 버전 (전 장 공통)
# ---------------------------------------------------------------
# 설치:
#   python -m venv .venv
#   .venv\Scripts\activate          # Windows PowerShell
#   source .venv/bin/activate       # macOS / Linux
#   pip install -r practice/requirements-student.txt
#
# 왜 버전을 못 박았나
#   버전을 열어 두면(>=) 설치하는 날짜에 따라 서로 다른 버전이 깔린다.
#   같은 코드를 돌려도 사람마다 결과나 오류가 달라져 수업이 멈춘다.
#   아래 버전 조합은 실제로 설치해서 실습 코드를 돌려 본 조합이다.
#
# 검증 기록
#   Windows 11 / Python 3.12.7 : 설치 성공, 1장 1-1 및 8/10/12/13장 표본 실행 성공
#                                (1장 출력값이 macOS 실행 로그와 완전히 일치)
#   macOS 14 / Python 3.14.6   : 집필 시 실행 로그의 출처 환경.
#                                단, 아래 고정 버전 조합으로는 아직 재설치 검증 안 함.
#   scipy==1.18.0              : 원래 scikit-learn에 딸려 들어오던 것을 명시로 바꿨다.
#                                2·3·6·9~13·15장이 scipy를 직접 import 한다. 집필
#                                환경에 깔려 있던 버전이 1.18.0이라 그 값으로 못 박았다.
#
# 이 파일로 덮이지 않는 것은 2장의 OSM 스냅샷 재생성 경로 하나뿐이다. 스냅샷은
# 이미 받아 둔 파일을 쓰므로 보통은 필요 없고, 새로 받을 때만 osmnx를 더 깐다.
#   python lecture_practice/chapter2/code/2-0b-prepare-osm-snapshot.py --refresh
#
# 딥러닝(PyTorch)은 용량이 커서 따로 뺐다. 5장과 7장에서만 필요하고, 5장에
# 들어갈 때 아래를 추가로 설치한다(6장은 PyTorch 없이 돌아간다).
#   pip install -r practice/requirements-student-dl.txt
# ---------------------------------------------------------------

# --- 공간 데이터 ---
geopandas==1.1.4
shapely==2.1.2
pyproj==3.7.2
rasterio==1.5.0
xarray==2026.7.0
pystac-client==0.9.0
pyarrow==25.0.0

# --- 공간 통계 ---
libpysal==4.15.0
esda==2.10.0

# --- 데이터 처리 · 머신러닝 ---
numpy==2.4.6
pandas==3.0.5
scipy==1.18.0
scikit-learn==1.9.0
statsmodels==0.14.6
xgboost==3.3.0
lightgbm==4.7.0

# --- 시각화 · 해석 ---
matplotlib==3.11.1
shap==0.52.0
