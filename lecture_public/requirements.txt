# GeoAI 강의 통합 실습 환경 (v2, 14장 공용)
# ---------------------------------------------------------------
# 단일 venv로 전 장 실습을 실행한다. (버전 충돌 없음 — 모두 하한 지정)
#   python -m venv .venv
#   source .venv/bin/activate        # macOS/Linux
#   # .venv\Scripts\activate         # Windows
#   pip install -r practice/requirements.txt
# ---------------------------------------------------------------

# --- 공간(Geo) 스택: Ch1-5, 9 ---
geopandas>=1.0.0
shapely>=2.0.0
pyproj>=3.6.0
rasterio>=1.3.0
xarray>=2024.1.0
pystac-client>=0.8.0
planetary-computer>=1.0.0
pyarrow>=14.0.0
libpysal>=4.9.0
esda>=2.5.0
networkx>=3.2          # 도로망 그래프 (Ch2 배달 권역)
osmnx>=2.0.0           # OpenStreetMap 도로망·건물 취득 (Ch2 준비 스크립트 전용)

# --- 머신러닝·통계 스택: Ch4-5, 8-13 ---
numpy>=1.26.0
pandas>=2.0
scikit-learn>=1.4.0
statsmodels>=0.14
xgboost>=2.0.0
lightgbm>=4.0.0

# --- 딥러닝 스택: Ch5-7 (CPU/GPU 자동 폴백) ---
torch>=2.0.0
torchvision>=0.15.0

# --- 시각화: 전 과정 ---
matplotlib>=3.8.0
shap>=0.52
