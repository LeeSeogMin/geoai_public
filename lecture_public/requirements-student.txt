# GeoAI 실습 - 학생 배포용 고정 버전 (1-4장, 8-13장)
# ---------------------------------------------------------------
# 설치:
#   python -m venv .venv
#   .venv\Scripts\activate          # Windows PowerShell
#   source .venv/bin/activate       # macOS / Linux
#   pip install -r practice/requirements-student.txt
#
# 왜 버전을 못 박았나
#   버전을 열어 두면(>=) 설치하는 날짜에 따라 서로 다른 버전이 깔린다.
#   같은 코드를 돌려도 사람마다 결과나 오류가 달라져 수업이 멈춘다.
#   아래 버전 조합은 실제로 설치해서 실습 코드를 돌려 본 조합이다.
#
# 검증 기록
#   Windows 11 / Python 3.12.7 : 설치 성공, 1장 1-1 및 8/10/12/13장 표본 실행 성공
#                                (1장 출력값이 macOS 실행 로그와 완전히 일치)
#   macOS 14 / Python 3.14.6   : 집필 시 실행 로그의 출처 환경.
#                                단, 아래 고정 버전 조합으로는 아직 재설치 검증 안 함.
#
# 5-7장 딥러닝은 용량이 커서 따로 뺐다. 5장에 들어갈 때 아래를 추가로 설치한다.
#   pip install -r practice/requirements-student-dl.txt
# ---------------------------------------------------------------

# --- 공간 데이터 ---
geopandas==1.1.4
shapely==2.1.2
pyproj==3.7.2
rasterio==1.5.0
xarray==2026.7.0
pystac-client==0.9.0
pyarrow==25.0.0

# --- 공간 통계 ---
libpysal==4.15.0
esda==2.10.0

# --- 데이터 처리 · 머신러닝 ---
numpy==2.4.6
pandas==3.0.5
scikit-learn==1.9.0
statsmodels==0.14.6
xgboost==3.3.0
lightgbm==4.7.0

# --- 시각화 · 해석 ---
matplotlib==3.11.1
shap==0.52.0
